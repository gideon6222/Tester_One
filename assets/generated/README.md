# assets/generated/

What the Smith made for this game: `<name>.glb`, `<name>_lod1.glb`,
`<name>_collision.glb`, the contact sheet, and `<name>.json` carrying the request, the
generator, the parameters, the seed, the triangle count and the judge's verdict.

Committed. Never edited by hand: change the request or the generator in
`C:\dev\asset-forge` and let the forge rebuild it, so the sidecar stays true.

Every file here needs a line in `../LICENSES.md`.
